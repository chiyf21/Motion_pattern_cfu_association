# Fig5/24 input-version audit

Spatial modules are computed from the Fig5/23 `input_links/cfu` files listed in the CSV. The temporal network is joined from Fig5/23 `empirical_FDR_significant_pairs.csv`, whose runner reads the same `input_links/cfu` directory. Pattern masks and activations come from the listed motion_pattern_cfu_association pattern `06_patterns/objects.pkl` files.
